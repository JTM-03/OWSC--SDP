# Environment Variables

# API Base URL
# Uses a relative path so requests go through the Vite dev proxy (no CORS).
# For production, set this to your real API domain: https://api.yourdomain.com/api
VITE_API_URL=/api/
