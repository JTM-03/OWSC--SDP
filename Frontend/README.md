
  # OWSC UI

  This is a code bundle for OWSC UI. The original project is available at https://www.figma.com/design/AxEeoTrqcwOoppLu0Kkmtn/OWSC-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  